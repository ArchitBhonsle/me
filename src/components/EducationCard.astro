---
const { topLeft, topRight, bottomLeft, bottomRight } = Astro.props;
---

<div class="flex flex-col gap-2">
  <div class="flex justify-between flex-wrap gap-1">
    <div class="font-bold">{topLeft}</div>
    <div>{topRight}</div>
  </div>
  <div class="flex justify-between items-end flex-wrap gap-1">
    <div class="italic">{bottomLeft}</div>
    <div class="text-sm">{bottomRight}</div>
  </div>
</div>
